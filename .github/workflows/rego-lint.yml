name: Rego Lint

on:
  push:
    branches:
      - task/rego-lint-action

jobs:
  rego-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: ./.github/actions/rego-lint
