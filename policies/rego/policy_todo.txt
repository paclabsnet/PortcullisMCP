Things to do



DONE - 1. We should include a deny table rule that uses the arg_restriction thing we just built

DONE - 2. we need the primary scaffold that parses any escalation_tokens and converts the valid ones
   into escalation_data elements in an array


DONE - 3. write some Raygun tests to verify that the rules are working as expected


DONE - 4. finish up the tabular example to include all of the scenarios (allow, escalate, deny, allow-after-escalate)


DONE 5. Implement the same rules in custom rego.  take advantage of the util functions


DONE 6. those same raygun tests should work for custom rules if the logic is the same

DONE 7. look at the rules for the enterprise mcp
DONE 8. have some escalation scenarios handy

DONE 9. add the 'and' restriction to the ladder, and implement it
DONE 10. add the 'suffix' restriction to the ladder, and implement it

DONE 11. write a test for the suffix restriction
DONE 12. implement the custom rules for fetch, and verify them
DONE 13. set up a pair of escalation rules for 'fetch' for the OpenPolicyAgent.org and styra.com websites.  For demonstration purposes



# Next steps

After all of these are done, we integrate

1. make keep call out to the PDP for answers
2. if the answer is deny, return that
3. if the answer is 'escalate', examine the response object.  if the response object includes escalation_scope, create a JWT from that scope and a URL that includes that JWT so the user can approve it
3.1. but if the user doesn't belong to an approved group, they should get a different response
3.2. claude also pointed out that perhaps group escalation shouldn't even be a thing? Suitable for demonstration but not for real-world implementations
4. if the answer is allow, pass the request on to the actual MCP for processing
5. get an escalate response properly set up
6. create the appropriate approval token
7. make sure this works in raygun tests
8. add the token to the official list
9. verify that the agent is able to access the MCP once the token is added

- 

# Future steps

## JWT ID
when we hit the escalate path and find a JWT that satisfies the escalation 
(so it becomes 'allow'), we should consider adding identity information about which
JWT was used to the response, so it is captured in the decision log.

## identity
Stop looking at the groups in the Principal. Create the Principal object from the input request if and 
only if the source_type is "os".  If the type is "oidc", create the principal object from the contents
of the valid OIDC token, which must be a field in the principal section.  



